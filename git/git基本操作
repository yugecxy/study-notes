一.从github拉项目到本地:
1.生成公秘钥
2.将公钥复制到github
3.下拉项目：
git clone http_address   :将gittub上面的项目克隆下来(http_address是项目的链接)

二.上传文件
git add files            :添加文件  --添加一个任务
git commit -m "dsfasda"  :执行第二部的“add”任务，将本地文件放缓存区
git push origin master   :将缓存区的数据提交到github

三.同步本地文件：
git pull origin master   :github上面更新数据了，本地的也会同步更新。


更换分支：
git checkout -b dev

查看所有分支和自己所在分支:
git branch 

查看commit提交状态:
git status

